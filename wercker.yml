box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.14
        theme: greyshade
        flags: --buildDrafts=true
deploy:
  steps:
  - gh-pages:
        token: $GIT_TOKEN
        domain: nonnen.io
        basedir: .
        repo: helvalius/helvalius.github.io
